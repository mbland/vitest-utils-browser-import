{
  "name": "vitest-utils-browser-import",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite build && vite preview",
    "test": "vitest",
    "test:workaround": "vitest -c vitest.config-workaround.js"
  },
  "devDependencies": {
    "@vitest/browser": "file:../vitest/packages/browser",
    "vite": "link:../vite/packages/vite",
    "vitest": "file:../vitest/packages/vitest",
    "webdriverio": "8.24.6"
  },
  "pnpm": {
    "overrides": {
      "vite": "$vite"
    }
  }
}
